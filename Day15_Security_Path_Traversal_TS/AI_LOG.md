# Day 15 — AI Usage Log

## Phase 1 — Think like an attacker (no AI)
Escape vectors and why `path.join` doesn't stop them:
- `../` relative escape:
- absolute path:
- mid-path `a/../../`:
- Why a bare `startsWith(base)` is itself a bug:

## Fix
| What I changed | Why it's safe | How I verified |
|----------------|---------------|----------------|
| | | |

## Prompt log
| Prompt (paraphrase) | Specific? | Output correct? | How I verified |
|---------------------|-----------|-----------------|----------------|
| | | | |

## Reflection
- Did AI's first suggestion use a separator-boundary check, or a naive prefix check?
- How would this generalize to your PromptGuard work (validating unsafe input before it acts)?

## Self-score (1–5)
- Scoping:  · Prompt quality:  · Verification:  · Ownership:  · Communication: 
