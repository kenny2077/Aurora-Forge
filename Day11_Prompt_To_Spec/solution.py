"""Day 11 — implement slugify to pass test_solution.py.

    slugify(title: str) -> str

The product ask is one fuzzy sentence (see README). Before coding, write a PRECISE spec in
prompt.md — precise enough that an AI couldn't implement it wrong — then implement and verify.
"""


def slugify(title):
    raise NotImplementedError("Write the precise spec first, then implement.")
