"""Day 34 — implement a Leaderboard to pass test_leaderboard.py.

The ticket is one vague sentence (see README). Fill in spec_notes.md with the questions you'd ask
BEFORE reading the tests, then implement.

    add_score(player, score) -> None
    top(n) -> list[str]
    rank(player) -> int
"""


class Leaderboard:
    def __init__(self):
        raise NotImplementedError("Scope it first (spec_notes.md), then implement.")

    def add_score(self, player, score):
        raise NotImplementedError

    def top(self, n):
        raise NotImplementedError

    def rank(self, player):
        raise NotImplementedError
