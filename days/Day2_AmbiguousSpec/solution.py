"""Day 2 — implement these to pass test_solution.py.

    wrap_lines(text: str, width: int) -> list[str]
    line_count(text: str, width: int) -> int

Fill in spec_notes.md BEFORE you read the tests. The exact behavior is intentionally
under-specified in the ticket — that's the exercise.
"""


def wrap_lines(text, width):
    raise NotImplementedError("Implement wrap_lines to pass the spec.")


def line_count(text, width):
    raise NotImplementedError("Implement line_count to pass the spec.")
